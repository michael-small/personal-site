// An HOC that allows housing a HTML element(s) without adding any styling/structure
// An alternative in certain scenearios to writing `<React.Framgment>`
const aux = (props) => props.children;

export default aux;
